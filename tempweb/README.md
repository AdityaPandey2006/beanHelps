# BeanHelps Temp Web

Temporary dependency-free frontend for demoing the backend endpoints that already exist.

Run the API first, then from this folder run:

```bash
npm start
```

Open:

```text
http://localhost:3000
```

This folder does not require changes inside `apps/api`. It uses the existing API base URL `http://localhost:5000/api` by default.
